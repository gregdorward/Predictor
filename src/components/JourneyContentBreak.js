/** Short paragraph between major sections so Journey can place in-content units. */
export default function JourneyContentBreak({ children }) {
  return <p className="FixturePage-contentBreak">{children}</p>;
}
